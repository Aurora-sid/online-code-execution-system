FROM python:alpine

# Install build dependencies if strictly necessary, but keep it minimal for runtime
# For simple scripts, python:alpine is enough.
WORKDIR /app

# The container will stay alive waiting for exec commands
CMD ["sleep", "infinity"]
